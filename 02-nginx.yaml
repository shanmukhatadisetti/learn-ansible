- name: Installing nginx
  hosts: frontend
  become: yes
  tasks:
  - name: Downloading nginx
    ansible.builtin.package:
      name: nginx
      state: present

  - name: Start server
    ansible.builtin.service:
      name: nginx
      enabled: yes
      state: started